<!-- auth.tmpl -->
{{ define "auth" }}
  <br>
  <input class="selectToken"
          name="{{ .Auth.Basic.Field }}"
   placeholder="{{ .Placeholder.Auth }}"
         title="authentication required"
          type="text" required>
{{ end }}
