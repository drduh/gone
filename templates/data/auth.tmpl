<!-- auth.tmpl -->
{{ define "auth" }}
<input required type="text"
               title="authentication required"
               class="selectToken"
                name="{{ .Auth.Header }}"
         placeholder="{{ .Auth.Holder }}">
<br>
{{ end }}
